.build/
.swiftpm/
DerivedData/
.DS_Store
# Xcode user-specific state (keep the .xcodeproj + shared schemes)
xcuserdata/
*.xcuserstate
# Secrets — never commit API keys
Secrets.plist
Secrets.local.swift
